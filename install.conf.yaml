- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.latexmkrc: latexmkrc
    ~/.zshrc: zshrc
    ~/.config/nvim: nvim


- shell:
  - [git submodule update --init --recursive, Installing submodules]

